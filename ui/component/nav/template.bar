<nav class=nav>
{{#iterate this}}
	<a
	 is="ui-link"
	 ui-hash="{{this.hash}}"
	 class="nav--link {{ifel this.isActive 'nav--link=active' ''}}"
	href="{{this.uri}}">{{this.title}}</a>
{{/iterate}}
</nav>